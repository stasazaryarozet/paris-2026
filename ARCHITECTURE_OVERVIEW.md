# Архитектурный обзор системы

**Дата создания**: 31 декабря 2024  
**Версия**: 1.0  
**Статус**: Актуальная архитектура проекта

---

## Общая архитектура

### Компоненты системы

```
┌─────────────────┐
│   Frontend      │
│  (index.html)   │
│   + content.js  │
└────────┬────────┘
         │ POST (FormData)
         ▼
┌─────────────────┐
│  Primary API    │  ← Приоритетный endpoint
│ api.parisin     │
│ january.ru/     │
│ api/submit      │
└────────┬────────┘
         │ Process & Forward
         ▼
┌─────────────────┐
│ Telegram Bot    │  ← Основной получатель
│   (API Token)   │
└─────────────────┘
         │
         ▼ (fallback)
┌─────────────────┐
│   Formspree     │  ← Резервный вариант
│  (email only)   │
└─────────────────┘
```

---

## Текущая конфигурация формы

### Primary Endpoint (Ожидаемый)
- **URL**: `https://api.parisinjanuary.ru/api/submit`
- **Метод**: POST
- **Content-Type**: `application/x-www-form-urlencoded` или `multipart/form-data`
- **Статус**: Кастомный backend (по документации)

### Fallback Endpoint (Текущий в коде)
- **URL**: `https://formspree.io/f/xvgwnvkb`
- **Метод**: POST
- **Статус**: Используется в `index.html` как action формы

---

## Данные, отправляемые формой

### Поля формы (из index.html)

```javascript
FormData {
  name: "Иван Иванов",              // Обяз. поле
  contact: "test@example.com",      // Обяз. поле (email или телефон)
  consent: "on",                    // Обяз. checkbox
  _subject: "Париж январь 2026 — заявка",
  _replyto: "info@parisinjanuary.ru",
  _next: "https://parisinjanuary.ru/?success=1"
}
```

### Валидация на клиенте

**Email Regex**: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`  
**Phone Regex**: `/^[\d\s\-\+\(\)]{7,}$/`

---

## Архитектура обработки (ожидаемая)

### Primary API Flow

1. **Получение запроса**
   ```
   POST /api/submit
   Content-Type: application/x-www-form-urlencoded
   
   name=Иван+Иванов&contact=test@example.com&consent=on&...
   ```

2. **Обработка на backend**
   - Валидация данных
   - Проверка anti-bot полей (_honey, _tstart если есть)
   - Подготовка уведомления

3. **Отправка в Telegram**
   ```json
   POST https://api.telegram.org/bot<TOKEN>/sendMessage
   {
     "chat_id": <CHAT_ID>,
     "text": "🆕 Новая заявка...",
     "parse_mode": "HTML"
   }
   ```

4. **Ответ клиенту**
   - 200 OK → форма показывает успех
   - 4xx/5xx → fallback на Formspree

---

## Информация, получаемая Telegram ботом

### Реальная структура данных (из index.html)

Telegram бот получает данные из формы через один из двух путей:

#### Путь 1: Primary API (`api.parisinjanuary.ru/api/submit`)

**Данные, отправляемые на backend:**
```javascript
{
  name: string,           // Имя клиента (обязательно)
  contact: string,        // Email или телефон (обязательно, валидируется)
  consent: "on",         // Согласие (обязательно, checkbox)
  _subject: "Париж январь 2026 — заявка",
  _replyto: "info@parisinjanuary.ru",
  _next: "https://parisinjanuary.ru/?success=1"
}
```

**Backend обрабатывает и отправляет в Telegram:**
- Все поля формы
- Дополнительно: timestamp, IP, user agent (если backend их добавляет)

#### Путь 2: Formspree Webhook (текущий в коде)

**Данные из Formspree JSON webhook:**
```json
{
  "name": "Иван Иванов",
  "contact": "test@example.com",
  "consent": "on",
  "_subject": "Париж январь 2026 — заявка",
  "_replyto": "info@parisinjanuary.ru",
  "_next": "https://parisinjanuary.ru/?success=1",
  "_form": "xvgwnvkb",
  "_format": "plain"
}
```

### Минимальный набор данных для Telegram бота

**Обязательные поля:**
1. `name` — имя клиента из формы
2. `contact` — email или телефон для связи
3. `consent` — подтверждение согласия (значение "on")

**Дополнительная информация (метаданные):**
- `_subject` — тема заявки
- `timestamp` — время отправки (добавляется backend/webhook)
- `source` — источник (parisinjanuary.ru)

### Формат сообщения в Telegram

```text
🆕 Новая заявка на бронирование

👤 Имя: Иван Иванов
📧 Контакт: test@example.com
✅ Согласие: Получено

📅 Тур: Париж январь 2026
⏰ Время: 31.12.2024 15:00
🌐 Источник: parisinjanuary.ru
```

---

## Текущее состояние (реальность vs документация)

### Документация указывает:
- Primary: `api.parisinjanuary.ru/api/submit`
- Fallback: `formspree.io/f/xvgwnvkb`

### Текущий код:
- Action формы: `formspree.io/f/xvgwnvkb` (прямая отправка)

### Вывод:
Есть расхождение между документацией и реализацией. Форма отправляет напрямую на Formspree, не используя primary endpoint.

---

## Рекомендации

### Для получения данных в Telegram бот:

**Вариант 1: Через Formspree Webhook**
1. Настроить webhook в Formspree
2. Указать URL Telegram бота
3. Получать данные в формате Formspree

**Вариант 2: Через собственный Backend API**
1. Настроить `api.parisinjanuary.ru/api/submit`
2. Обработать запрос
3. Отправить в Telegram через Bot API
4. Вернуть ответ клиенту

**Вариант 3: Изменить action формы**
1. Изменить `action` в `index.html` на primary endpoint
2. Реализовать fallback логику в JavaScript
3. Если primary недоступен → отправка на Formspree

---

## Архитектурные принципы

1. **Single Source of Truth**: WEBSITE_CONTENT.md для контента
2. **Minimalism**: Простота превыше сложности
3. **Reliability**: Fallback механизмы для критических путей
4. **Privacy**: Защита персональных данных (GDPR/ФЗ-152)

---

## Связанные файлы

- `index.html` — форма и JavaScript обработчик
- `FORM_CHECK_PROCEDURE.md` — процедура проверки формы
- `HANDSHAKE_V4_RESPONSE.md` — конфигурация endpoints
- `PROJECT_KNOWLEDGE.md` — общая архитектура проекта

---

**Последнее обновление**: 31 декабря 2024  
**Статус интеграции Telegram**: Требует уточнения текущей реализации

---

## Связанные материалы

- `screenshot` — скриншот (PNG, 1170x1022) в корне проекта, содержит визуальную информацию для проверки
- `FEEDBACK.md` — ссылается на скриншот для проверки визуального соответствия
- `source_materials/Screenshot 2025-10-16 at 17.20.30.png` — архивный скриншот

---

**Примечание**: Скриншот `screenshot` в корне проекта может содержать визуальное представление:
- Интерфейса Telegram с данными, полученными ботом
- Архитектуры системы с путями передачи данных
- Формата сообщений, которые получает Telegram бот

Для полного понимания архитектуры рекомендуется просмотреть скриншот напрямую.
